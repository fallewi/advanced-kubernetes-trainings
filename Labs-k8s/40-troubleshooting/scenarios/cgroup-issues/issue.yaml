apiVersion: v1
kind: Pod
metadata:
  name: cgroup-issue-pod
spec:
  containers:
  - name: stress
    image: polinux/stress
    command: ["stress", "--vm", "1", "--vm-bytes", "100M"]
    resources:
      limits:
        memory: "50Mi"
