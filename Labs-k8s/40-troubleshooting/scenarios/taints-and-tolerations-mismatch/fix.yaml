apiVersion: v1
kind: Pod
metadata:
  name: taints-tolerations-fixed-pod
spec:
  containers:
  - name: busybox
    image: busybox
    command:
      - "sh"
      - "-c"
      - "echo 'Taints and tolerations fixed' && sleep 3600"
