apiVersion: v1
kind: Pod
metadata:
  name: firewall-restriction-fixed-pod
  labels:
    app: firewall-test
spec:
  containers:
  - name: busybox
    image: busybox
    command: ["sh", "-c", "wget -qO- --timeout=5 http://google.com || echo 'no connectivity'; sleep 3600"]
