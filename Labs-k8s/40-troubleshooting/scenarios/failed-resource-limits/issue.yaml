apiVersion: v1
kind: Pod
metadata:
  name: failed-resource-limits-pod
spec:
  containers:
  - name: stress
    image: polinux/stress
    command: ["stress", "--vm", "1", "--vm-bytes", "64M"]
    resources:
      limits:
        memory: "32Mi"
