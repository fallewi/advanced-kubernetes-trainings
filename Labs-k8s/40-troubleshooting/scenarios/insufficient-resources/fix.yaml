apiVersion: v1
kind: Pod
metadata:
  name: insufficient-resources-fixed-pod
spec:
  containers:
  - name: busybox
    image: busybox
    command: ["sh", "-c", "sleep 3600"]
    resources:
      requests:
        cpu: "100m"  # Adjusted resource request
        memory: "64Mi"
