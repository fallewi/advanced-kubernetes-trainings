apiVersion: v1
kind: Pod
metadata:
  name: pid-namespace-collision-fixed-pod
spec:
  containers:
  - name: busybox
    image: busybox
    command: ["sh", "-c", "echo 'PID namespace isolated' && sleep 3600"]
    securityContext:
      runAsUser: 1000
