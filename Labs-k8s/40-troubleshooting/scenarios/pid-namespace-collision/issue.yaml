apiVersion: v1
kind: Pod
metadata:
  name: pid-namespace-collision-pod
spec:
  hostPID: true
  containers:
  - name: busybox
    image: busybox
    command: ["sh", "-c", "echo 'WARNING: Host PID namespace shared - security risk' && sleep 3600"]
    securityContext:
      runAsUser: 1000
