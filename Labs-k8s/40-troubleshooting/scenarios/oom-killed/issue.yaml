apiVersion: v1
kind: Pod
metadata:
  name: oom-killed-pod
spec:
  containers:
  - name: stress
    image: polinux/stress
    resources:
      limits:
        memory: "50Mi"
      requests:
        memory: "50Mi"
    command: ["stress", "--vm", "1", "--vm-bytes", "100M"]
